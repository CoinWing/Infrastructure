apiVersion: v1
kind: Service
metadata:
  name: pending-queue-redis
  namespace: cowing-prod
  labels:
    app: pending-queue-redis
spec:
  selector:
    app: pending-queue-redis
  ports:
  - protocol: TCP
    port: 6381
    targetPort: 6381
  type: ClusterIP