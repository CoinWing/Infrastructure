apiVersion: v1
kind: Service
metadata:
  name: msa-auth
  namespace: cowing-prod
spec:
  selector:
    app: msa-auth
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082
  type: ClusterIP
