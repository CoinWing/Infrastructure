apiVersion: v1
kind: Pod
metadata:
  name: msa-auth-redis
  namespace: cowing-prod
  labels:
    app: msa-auth-redis
spec:
  containers:
    - name: msa-auth-redis
      image: redis:alpine
      command: ["sh", "-c"]
      args:
        - redis-server --appendonly no --save ''
      resources:
        limits:
          memory: 200Mi
          cpu: 200m
        requests:
          memory: 100Mi
          cpu: 100m
      ports:
        - containerPort: 6379